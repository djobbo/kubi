{
  "name": "@dair/api",
  "scripts": {
    "dev": "bun run db:push && bun run --hot src/index.ts",
    "db:generate": "drizzle-kit generate",
    "db:push": "bun db:generate && drizzle-kit push",
    "studio": "bunx drizzle-kit studio"
  },
  "dependencies": {
    "@dair/brawlhalla-api": "workspace:*",
    "@dair/brawlhalla-servers": "workspace:*",
    "@dair/schema": "workspace:*",
    "@t3-oss/env-core": "^0.13.6",
    "better-sqlite3": "^11.10.0",
    "drizzle-orm": "^0.44.2",
    "hono": "^4.7.11",
    "zod": "^3.25.56"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "drizzle-kit": "^0.31.1"
  }
}